import Navbar from "./components/navbar";

const Home = () => {
    return (
        <main>
            <Navbar />
            <section>
                Section
            </section>
        </main>
    );
};

export default Home;